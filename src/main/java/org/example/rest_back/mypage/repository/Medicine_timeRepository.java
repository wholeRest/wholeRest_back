package org.example.rest_back.mypage.repository;

import org.example.rest_back.mypage.entity.Medicine_time;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Medicine_timeRepository extends JpaRepository<Medicine_time, Integer> {
}
